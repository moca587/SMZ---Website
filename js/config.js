// SMZ backend connection.
// Fill these in after creating the Supabase project (see SETUP.md).
// Both values are safe to commit: the anon key is public by design and the
// row level security policies in supabase/schema.sql are what protect the data.
// While they are empty the site simply shows its built in content.
window.SMZ_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
